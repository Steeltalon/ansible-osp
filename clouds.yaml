clouds:
  ospcloud:
    auth:
      auth_url: "{{ auth_url }}"
      password: r3dh4t1!
      project_name: admin
      username: admin
    identity_api_version: '3'
    region_name: RegionOne
ansible:
  use_hostnames: True
  expand_hostvars: False
  fail_on_errors: True
